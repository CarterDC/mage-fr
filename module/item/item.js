/**
 * Extend the basic Item with some very simple modifications.
 * @extends {Item}
 */
export default class M20eItem extends Item {

  /** @override */
  constructor(...args) {
    super(...args);
  }

}
